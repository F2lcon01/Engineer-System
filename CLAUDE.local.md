# CLAUDE.local.md — ملاحظات السياق المحلية

> **هذا الملف للمشروع الحالي فقط.**
> - يُنشأ مرة عند `install-eng`
> - يحدّثه `/bootstrap` تلقائياً
> - **محمي من الـ upgrades** (لا يُكتب فوقه)
> - **مُتجاهَل في git** (لا يُرفع — يبقى محلياً)
> - يُقرأ تلقائياً مع `CLAUDE.md` من Claude Code

---

## هوية المشروع

- **الاسم:** [يُملأ بـ /bootstrap]
- **الهدف:** [يُملأ بـ /bootstrap]
- **الـ stack الأساسي:** [يُملأ بـ /bootstrap]

## التفضيلات

- لغة التواصل: العربية (إذا غير ذلك، عدّل هنا)
- نمط الـ commit messages: [Conventional / Angular / حر]
- معايير الكود الخاصة: [أي قواعد لا توجد في linter]

## ملاحظات تشغيل خاصة

- أمر التشغيل المحلي: [مثلاً: `npm run dev`]
- أمر النشر: [مثلاً: `git push origin main`]
- ملفات حساسة لا تُلمَس: [قائمة]

## السياق التاريخي

[أي معرفة عن المشروع لن يكتشفها كلود من الكود وحده — قرارات قديمة، أسباب اختيارات، إلخ.]
